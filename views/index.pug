extends layout
block content
  h1 Welcome!
  if (!user)
    a(href="/login") 
      button Login
    br
    br
    a(href="/register") 
      button Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/organization") Take me to Organization
    br
    a(href="/logout") Logout